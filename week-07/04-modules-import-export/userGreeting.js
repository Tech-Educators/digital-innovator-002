// the 'export default' keywords here state that "this is the default thing to be exported", allowing us to use the syntax without curly brackets.
export default function greetsUser(userName) {
  return `Hello ${userName}`;
}
